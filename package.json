{
  "name": "tracker",
  "version": "1.0.0",
  "main": "tracker.js",
  "type": "module",
  "scripts": {
    "start": "node tracker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "keywords": [
    "torrent",
    "tracker",
    "bittorrent"
  ],
  "author": "Akash", 
  "license": "ISC",
  "description": "A BitTorrent tracker server implemented with bittorrent-tracker",
  "dependencies": {
    "bittorrent-tracker": "^11.1.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0"  // Optional: add eslint for linting if desired
  }
}
